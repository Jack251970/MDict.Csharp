<Window
    x:Class="MDict.Csharp.Demo.WPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MDict.Csharp.Demo.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    Title="MDict.Csharp Demo"
    Width="800"
    Height="450"
    Closed="Window_Closed"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                x:Name="Path"
                Grid.Column="0"
                Margin="8,8,4,4"
                IsReadOnly="True" />
            <Button
                x:Name="PathButton"
                Grid.Column="1"
                Margin="4,8,8,4"
                Command="{Binding NavigatePathCommand}"
                Content="Select" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Margin="8,4,4,4"
                VerticalAlignment="Center"
                Text="Search text:" />
            <TextBox
                x:Name="Search"
                Grid.Column="1"
                Margin="4,4,8,4"
                VerticalAlignment="Center"
                TextChanged="Search_TextChanged" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="7*" />
            </Grid.ColumnDefinitions>

            <ListView
                x:Name="FuzzyWordListView"
                Margin="8,4,4,8"
                ItemsSource="{Binding FuzzyWords}"
                SelectionChanged="FuzzyWordListView_SelectionChanged"
                SelectionMode="Single" />
            <wv2:WebView2
                x:Name="ResultWebView2"
                Grid.Column="2"
                Margin="4,4,8,8" />
        </Grid>
    </Grid>
</Window>
